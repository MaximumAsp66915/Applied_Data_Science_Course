# ---------------------------------------------------------------------------
# Recommendation engine microservice (v2, ensemble) -- deliberately its own
# requirements file, installed into its own venv (engine/.venv) on the
# server, completely separate from app/requirements.txt. See README.md for
# why, and setup_venv.sh to create the venv and install these.
# ---------------------------------------------------------------------------

# Serving
fastapi==0.115.5
uvicorn[standard]==0.32.0

# Inference over the exported model_params/ artifacts only. No torch:
# inference only ever does precomputed-embedding dot products (see
# recommender.py's docstring and Manual.txt section 4) -- artist_model_state.pt
# / track_model_state.pt / genre_features.pt are not loaded by this service.
numpy==1.26.4

# Required to unpickle model_params/*_enc*.pkl -- they're
# sklearn.preprocessing.LabelEncoder instances, so sklearn must be installed
# to load them even though only their plain .classes_ array is actually read.
scikit-learn==1.5.2
